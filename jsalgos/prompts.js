// asks question expecting response

let birth = prompt("Your age?", "20");

console.log(birth);

//Even though the user will respond with a number, the answer will come back as a string
// the second string is the default answer
// Even if the user clicks off w/o an answer, the 2nd string will be the default
// You can assign a variable to set as default answer

