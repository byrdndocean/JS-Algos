// The basic of basic way to figure something out in JS
// older way of doing things

let b = prompt("What's the normal color of the sky?");
if (b == "blue") {
  alert("True Indeed");
}

//can use a variable instead of a string

//capitalization matters

// !== means not equal
// || or
// > greater than
// < less than
// >= is greater than or equal to
// <= less than or equal to